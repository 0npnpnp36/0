# Eye scroll prototype

Archived on July 16, 2026. 

It is intentionally not active in `src/`.

To restore the prototype from the repository root:

```powershell
git apply prototypes/eye-scroll/eye-scroll.patch
```

To remove it again:

```powershell
git apply -R prototypes/eye-scroll/eye-scroll.patch
```

